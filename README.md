# server-backend
Server backend code for the interactive Mars Analysis and Research System

productid.py is a CGI script based on MapScript (http://mapserver.org). It intercepts the OWS request and uses the parameter PRODUCTID as the file path for the image map.
